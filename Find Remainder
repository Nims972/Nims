#include<stdio.h>
int main()
{
    int no,x[1000],y[1000],z[1000];
    scanf("%d",&no);
    for(int i=0;i<no;i++)
    {
        scanf("%d %d",&x[i],&y[i]);
        z[i]= x[i] % y[i];
    }
    for(int i=0;i<no;i++)
    {
        printf("%d\n", z[i]);
    }
    return 0;
 
}
