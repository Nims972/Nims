#include<stdio.h>
int main()
{
    int no,x[1000],y[1000];
    scanf("%d",&no);
 
    for(int i=0;i<no;i++)
    {
        scanf("%d",&x[i]);
        y[i] =(x[i]/2)+1;
    }
    for(int i=0;i<no;i++)
    {
        printf("%d\n",y[i]);
    }
 
 
    return 0;
}
 
